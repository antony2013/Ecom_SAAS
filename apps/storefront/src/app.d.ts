declare global {
  namespace App {
    interface Locals {
      customerId?: string;
      storeId?: string;
    }
    // interface Error {}
    // interface PageData {}
    // interface PageState {}
    // interface Platform {}
  }
}

export {};